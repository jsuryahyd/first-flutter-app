import 'package:flutter/material.dart';

class ProductListPage extends StatelessWidget {
  Widget build(context) {
    return Container(
      child: Center(child:Text('Your List of Products here.')),
    );
  }
}
