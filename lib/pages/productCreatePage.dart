import 'package:flutter/material.dart';

class ProductCreatePage extends StatelessWidget {
  Widget build(context) {
    return Container(
      child: Center(child:Text('Create a Product here.')),
    );
  }
}
